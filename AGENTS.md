for backend, use uv for dependency management. a few useful commands:

uv sync
uv add <PACKAGE-NAME>
uv run python <PYTHON-FILE>

regularly commit code to git

the code was written by an external freelance i don't necessarily trust

use the red-green-refactor approach when writing code so you start with the tests